<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.BlinkProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/button_pressed</item>
        <item name="colorSecondary">@color/button_not_pressed</item>
        <item name="background">@color/background</item>
    </style>

    <style name="Theme.BlinkProject" parent="Base.Theme.BlinkProject" />
</resources>